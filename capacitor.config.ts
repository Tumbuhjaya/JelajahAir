import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'tjd.jelajah.air',
  appName: 'jelajahAir',
  webDir: 'dist',
  server: {
    androidScheme: 'http'
  }
};

export default config;
